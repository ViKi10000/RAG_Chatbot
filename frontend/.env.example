# Frontend environment variables
# Copy to .env and set values. Vite only exposes variables prefixed with VITE_.

# API base URL (default: http://localhost:8000 for local dev)
# For Render production: https://rag-chatbot-backend.onrender.com
VITE_API_URL=http://localhost:8000

# Optional: default models used when calling /init (must match backend EMBEDDING_MODEL / GROQ_LLM_MODEL if you set those)
# VITE_EMBEDDING_MODEL=all-MiniLM-L6-v2
# VITE_LLM_MODEL=llama-3.1-8b-instant
