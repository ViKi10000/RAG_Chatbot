# Frontend environment variables
# Copy this file to `.env`. Vite only exposes variables prefixed with VITE_.

############################
## Local development      ##
############################

# Backend API URL for local dev (FastAPI runs on port 8000 by default)
VITE_API_URL=http://localhost:8000

############################
## Production examples    ##
############################

# When you deploy, override VITE_API_URL in your hosting env
# to point to the public backend URL (no :8000, just the HTTPS URL).
# Example:
# VITE_API_URL=https://your-backend-service.onrender.com

############################
## Optional model config  ##
############################

# Default models used when the frontend calls /init.
# These should match the backend EMBEDDING_MODEL / GROQ_LLM_MODEL
# if you override them there.
# VITE_EMBEDDING_MODEL=all-MiniLM-L6-v2
# VITE_LLM_MODEL=llama-3.1-8b-instant