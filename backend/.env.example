# Backend environment variables
# Copy to .env and fill in values. Do not commit .env.

# Required: Groq API key for LLM
GROQ_API_KEY=

# Optional: Groq LLM model name (default: llama-3.1-8b-instant)
GROQ_LLM_MODEL=llama-3.1-8b-instant

# Optional: Sentence embedding model (default: all-MiniLM-L6-v2)
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Optional: ChromaDB collection and persist path
CHROMA_COLLECTION_NAME=documents
CHROMA_PERSIST_DIR=./data/vector_store

# Optional: Document chunking for RAG
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Optional: Domain and generation config
DOMAIN=general
DOMAIN_PROMPT=You are a helpful AI assistant.
MAX_CONTEXT=3000
TEMPERATURE=0.7
MAX_TOKENS=1024

# Optional: Data directory to load on startup (default: ./data)
DATA_DIR=./data

# Optional: Server (default: 0.0.0.0:8000). PORT is set by Render.
HOST=0.0.0.0
PORT=8000

# Optional: CORS origins, comma-separated (default: * allows all)
# CORS_ORIGINS=https://your-frontend.onrender.com
